# Base URL for the Laravel REST API. Defaults to /api if unset — see
# src/api/client.ts. Point this at your backend once it exists, e.g.
# VITE_API_BASE_URL=https://api.zou.ac.zw/v1
VITE_API_BASE_URL=/api
